const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // Might not exist, let's see. Wait, I am in the local dev environment, I might not have firebase-admin credentials.
